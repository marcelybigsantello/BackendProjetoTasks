{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx nodemon --inspect server.mjs",
    "build": "npx nodemon --exec \"npm run build && node lib/index.js\" -e js --ignore lib/"
  },
  "keywords": [],
  "author": "Marcely Biguzzi Santello",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "nodemon": "^3.1.0",
    "sqlite": "^5.1.1",
    "sqlite3": "^5.1.7"
  }
}
